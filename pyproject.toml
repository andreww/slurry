[project]
name = "slurry"
version = "1.0.1-alpha"
description = "Modelling the F-layer in Earth’s core as a non-equilibrium slurry"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "adjusttext>=1.3.0",
    "jupyter>=1.1.1",
    "matplotlib>=3.10.8",
    "numba>=0.65.0",
    "numpy>=2.4.4",
    "pandas>=3.0.2",
    "scipy>=1.17.1",
]
